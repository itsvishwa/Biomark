import 'package:flutter/material.dart';

class RegisterPageStep2 extends StatelessWidget {
  const RegisterPageStep2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("Page 02")],
    );
  }
}
