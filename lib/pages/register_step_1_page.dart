import 'package:flutter/material.dart';

class RegisterPageStep1 extends StatelessWidget {
  const RegisterPageStep1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text("Page 01")],
    );
  }
}
